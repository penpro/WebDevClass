# Copy this file to .env.production (or .env.local for dev) and fill in
# your real API Gateway invoke URL. The .env files themselves are
# gitignored so secrets don't leak into the repo.
VITE_API_URL=https://abc123.execute-api.us-east-1.amazonaws.com
